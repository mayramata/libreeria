require('../connectiondb');
//se usa la lista d elos alumnos 
const Product = require('../models/schemaStudent');

const someFunction = async () => {
    // const deletedProduct = await Student.deleteOne({name: 'laptop hp'}); //emiminara el primero de muchos con el mismo nombre{busca todos y elimina uno}
    // const result = await Student.deleteMany({name: 'laptop hp'}); //elimina todos
    // const result = await Student.findOneAndDeletmousee({name: 'mouse'}); //busca un solo dato y elimina uno{busca uno y elimina uno}
    //const result = await Student.findByIdAndDelete('5e290e03a1def223dc11c70d'); //se pasa el id para eliminarlo
    

    console.log(result);

};

someFunction();